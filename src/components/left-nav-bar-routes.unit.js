import LeftNavBarRoutes from './left-nav-bar-routes'

describe('@components/left-nav-bar-routes', () => {
  it('exports a valid component', () => {
    expect(LeftNavBarRoutes).toBeAComponent()
  })
})
